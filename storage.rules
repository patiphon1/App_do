rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /posts/{userId}/{fileName} {
      allow read: if true;  // โหลดรูปได้ทุกคน
      allow write: if request.auth != null && request.auth.uid == userId; // เจ้าของเท่านั้นอัปได้
    }
  }
}
